const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
function bar(value,max,label){const width=max?Math.max(4,Math.round((value/max)*100)):0;return `<div class="mini-bar-row"><span>${esc(label)}</span><div class="mini-bar"><div style="width:${width}%"></div></div><strong>${value}</strong></div>`}
fetch('data.json').then(r=>r.json()).then(d=>{document.getElementById('biz').textContent=`${d.business.name} — ${d.business.location}`;document.getElementById('window').textContent=`Latest ${d.window.days} days · ${d.window.start} to ${d.window.end}`;const mode=document.getElementById('modeBadge');mode.textContent=d.data_mode==='real'?'LIVE CUSTOMER DATA':'DEMO DATA';mode.className=`mode-badge ${d.data_mode==='real'?'live':'demo'}`;const m=d.metrics;const cards=[['Reviews',m.reviews,'latest 30 days'],['Avg rating','⭐ '+m.average_rating,'out of 5'],['Positive',m.positive_pct+'%','4–5 star reviews'],['Negative',m.negative_pct+'%','1–2 star reviews'],['Alerts',m.alerts,'7-day anomaly checks']];document.getElementById('metrics').innerHTML=cards.map(x=>`<div class="card metric"><div class="k">${x[0]}</div><div class="v">${x[1]}</div><div class="metric-note">${x[2]}</div></div>`).join('');const cats=[...d.category_stats].filter(x=>x.mentions).sort((a,b)=>b.negative-a.negative).slice(0,8);document.getElementById('issues').innerHTML=`<table><tr><th>Issue</th><th>Mentions</th><th>Negative</th><th>Neg. rate</th></tr>${cats.map(x=>`<tr><td><a href="reviews.html?category=${encodeURIComponent(x.category)}">${esc(x.category)}</a></td><td>${x.mentions}</td><td>${x.negative}</td><td>${Math.round(x.negative_rate*100)}%</td></tr>`).join('')}</table>`;const b=d.brief;document.getElementById('brief').innerHTML=`<div class="brief-headline">${esc(b.headline)}</div><p>${esc(b.summary)}</p><div class="brief-label">Top concerns</div><div class="chip-row">${b.top_concerns.map(x=>`<span>${esc(x)}</span>`).join('')}</div><div class="brief-label">Recommended actions</div><ol>${b.recommended_actions.map(x=>`<li>${esc(x)}</li>`).join('')}</ol>${d.ai_owner_brief?`<hr><div class="brief-label">AI advisor</div><p>${esc(d.ai_owner_brief)}</p>`:''}`;if(!d.monthly_trend?.length){document.getElementById('trend').innerHTML='<p>No trend data yet.</p>'}else{const mx=Math.max(...d.monthly_trend.map(x=>x.reviews));document.getElementById('trend').innerHTML=d.monthly_trend.slice(-6).map(x=>bar(x.reviews,mx,`${x.month} · ⭐ ${x.average_rating}`)).join('')}document.getElementById('alerts').innerHTML=d.alerts.length?d.alerts.map(a=>`<div class="alert-box"><div class="alert-top"><strong>${esc(a.category)}</strong><span>${esc(a.severity)}</span></div><p>${a.current_negative} negative mentions in the latest 7 days vs ${a.previous_negative} in the prior 7 days.</p><a href="reviews.html?category=${encodeURIComponent(a.category)}&sentiment=Negative">View evidence →</a></div>`).join(''):'<p>No issue crossed the alert threshold this week.</p>';document.getElementById('menu').innerHTML=`<table><tr><th>Dish</th><th>Mentions</th><th>Positive</th><th>Negative</th><th>Avg rating</th></tr>${d.menu_stats.slice(0,12).map(x=>`<tr><td>${esc(x.item)}</td><td>${x.mentions}</td><td>${x.positive_pct}%</td><td>${x.negative_pct}%</td><td>${x.avg_rating}</td></tr>`).join('')}</table>`});